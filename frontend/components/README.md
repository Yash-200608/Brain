# Components

Reusable React components for the Jarvis dashboard. The current dashboard is a
single-file vanilla-JS implementation under `frontend/dashboard/`. As the UI
grows, extract reusable pieces here:

- `MessageList.jsx`
- `MemoryPanel.jsx`
- `GoalCard.jsx`
- `GraphView.jsx`
